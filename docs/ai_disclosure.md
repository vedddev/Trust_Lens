# AI tools disclosure

TrustLens supports an OpenAI-compatible API configured with `AI_API_KEY`, `AI_BASE_URL`, and `AI_MODEL`. The current baseline uses deterministic scoring and reserves provider-backed structured enrichment. AI-assisted development tools drafted code and documentation; output was reviewed and modified. TrustLens never makes definitive fraud accusations from AI inference alone.
